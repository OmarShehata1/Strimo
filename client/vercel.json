{
  "version": 2,
  "buildCommand": "ROLLUP_SKIP_NODEJS=true npm install --no-optional && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}